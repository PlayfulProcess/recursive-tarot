{
  "name": "recursive-recording",
  "version": "0.1.0",
  "type": "module",
  "description": "Chrome (Playwright) recording stack: performance grammar + audiobook/TTS → video + timestamped sequence grammar.",
  "bin": { "rr-record": "./recorder.mjs" },
  "scripts": {
    "setup": "playwright install chromium",
    "record": "node recorder.mjs",
    "orchestrate": "node orchestrate.mjs"
  },
  "dependencies": {
    "playwright": "^1.47.0",
    "ffmpeg-static": "^5.2.0"
  },
  "optionalDependencies": {
    "@google-cloud/text-to-speech": "^5.3.0"
  },
  "engines": { "node": ">=18" }
}
