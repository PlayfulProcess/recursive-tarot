# Batch plan — Sunday 2026-07-05 (rev. 2, replanned against latest `dev`)

> **Shipped 2026-07-05** (commit on `claude/batch-planning-budget-qdsd1a`): items **1, 2, 3, 4, 5**
> all landed. W1 ladder Rungs 6–8 in how-to-contribute; W2 all 19 courses registered + sources.html
> index (site-header `?v=43` across 27 pages); W3 `template/` starter kit (index renderer verified in
> headless Chromium, sample grammar, check.py, README); W5 CONTRIBUTORS.md (surfaced Julia as a real
> 2nd contributor); forest-of-hearts logo placed + animated (agent also fixed an invalid-UTF-8 byte in
> the SVG). **Item 6 (R2 migration) skipped** — no recursive-eco `.env.local`/R2 creds in this env.
> **Item 7 (W3 Session B) SHIPPED too** (`e448d5c`): `course/build-a-site-like-this-one.mdx` (Rung 7),
> registered in the menu + sources index, Rung 7 section in how-to-contribute updated to link it,
> site-header bumped `?v=44` across 27 pages. All of it in **PR #35 → `dev`**.
> Also audited-out of FUTURE_PLAN: the UUIDv5 import script (superseded — all 37 real grammars already
> mapped in `_eco_ids.json`) and the Phase 2 validation Action (`validate-grammar.yml` already exists;
> only the owner-only branch-protection click remains).
> Deferred within W2: per-course `duration`/prereq front-matter backfill (low value, needs per-course reads).


> Rev. 1 of this plan was written from a stale clone (dev @ Jun 15). Everything it proposed
> — drag-drop dims, Mamluk images, minor_key, Explorer P3, the playable game — has since
> shipped or been superseded. This revision is planned against `dev` @ `f23518a` (Jul 3).

**Budget context (10:25 today).** Max 5x: **all-models weekly bar 57% used (43% left)**,
**Fable 73% used (27% left)**, both reset **Monday 12:59 PM** — tomorrow. The requested
"50% of max" isn't available on the shared bar; the realistic ceiling is the remaining ~43
points, and anything unspent is lost at reset. Target: **~35–38 points today**, small buffer
for Monday morning.

**Routing rule.** Execution on **Opus** (judgment/extraction work) and **Sonnet** (mechanical,
well-specified). **No execution sessions on Fable** — its 27% is reserved for orchestration
and review. Heuristics: heavy Opus session ≈ 8–12 pts; focused Sonnet session ≈ 3–5 pts.

---

## The current frontier (audited Jul 5 against code, not just docs)

The old queues (TODO.md, NEXT-SESSION-DELEGATION, DEV-BRANCH-PLANS, BOOK-VIEWER-NEXT) are
essentially cleared — TODO.md is all ✅, the playable games shipped (Tarocchino/Trionfi/
Ma Diao), courses are grammars, the wishlist page is live. What's actually open:

| # | Item | Source | Verified open by | Model | Est. pts |
|---|------|--------|------------------|-------|----------|
| 1 | **W1 — stitch the graduation ladder**: add Rung 6 "Found your own commons" to `how-to-contribute.mdx` (hand-off to the nara 0→1 course), Rung 7/8 stubs, register the ladder in `COURSE_GROUPS` | PLAN-course-graduation-ladder | no "Rung 6"/"found your own" in the mdx | **Opus** (writing voice) | 6 |
| 2 | **W2 — course curation**: 19 course MDX, only ~7 registered; decide each one's fate (register / fold / archive), per-door reading orders, duration+prereq front-matter | same | `COURSE_GROUPS` vs `course/*.mdx` count | **Opus** (curation judgment) | 8 |
| 3 | **W3 Session A — fork-the-site starter kit**: extract theme.css, emptied site-header, course-viewer + one sample grammar, check_all, README ("A website like this one", Rung 7) | same | nothing extracted yet | **Opus** (extraction judgment) | 10–12 |
| 4 | **W5 — CONTRIBUTORS.md** + contribute-door section (CC-BY-SA attribution made visible) | same | file missing | Sonnet | 2 |
| 5 | **Logo placement + motion**: put `forest-of-hearts-logo.svg` in the index footer/shop header; give it the hero-spiral draw-on animation (stroke-dashoffset, ~3s, reduced-motion honored) | PLAN-preorder-and-logo §B | zero references to the logo in any page | Sonnet | 4 |
| 6 | **R2 migration, the stragglers**: 11 deck grammars still hotlink Commons `Special:FilePath` (slow, rate-limited) → `scripts/rehost_to_r2.py`, update `_collection` covers | NEXT-SESSION-DELEGATION S2 | grep count = 11 | Sonnet | 5 |
| 7 | Stretch — **W3 Session B**: the `build-a-site-like-this-one.mdx` course on top of the starter kit | PLAN-course-graduation-ladder | — | Opus | 8 |

**Core total (1–6): ~36 pts.** Fits the remaining 43 with buffer; item 7 only if the bar allows.

## Execution order

1. **Parallel first wave:** items 1+2 as one Opus session (same file family, same voice);
   items 4, 5, 6 as independent Sonnet lanes (disjoint paths, no conflicts).
2. **Second wave:** item 3 (the starter-kit extraction — the biggest judgment job; benefits
   from wave-1's course registry being settled).
3. **Only if points remain:** item 7.

Every lane: `python scripts/check_all.py` green before commit; `git pull --rebase origin dev`
before push (CI meta-rebuild commits land on `dev`). Item 6 needs the R2 creds from the
recursive-eco `.env.local`s (never echoed) — skip that lane if this environment lacks them.

## Blocked / not today

- **Pre-order & notify-me (PLAN-preorder-and-logo §A)** — needs a recursive-eco endpoint +
  Supabase `preorders` table; this session is scoped to recursive-tarot only. Do it from a
  session with the recursive-eco repo attached (it's the plan's own "1 desktop session").
- **W4 course completions** — explicitly gated on app availability (post-trip), per the plan.
- **Rung 8 / channels / memberships** — DEPENDS on trip Sessions 1–2 (app repo).
- Old NEXT-SESSIONS print/vendor queue (sampler proof, TGC upload) — gated on the builder's
  physical proof + `TGC_PASSWORD`, not on model budget.

## What ~36 points buys, in one line

By tonight: the contribution ladder is whole (Rung 6 links the nara course, the course menu
is a curated curriculum, contributors get a name-page), the forest-of-hearts logo is live and
breathing like the hero, no deck image still leans on rate-limited Commons, and the
"fork-the-site" starter kit exists — the Rung 7 deliverable — with Fable untouched.
